post_install() {
  echo "==> Waybar configuration has been installed to /etc/skel/.config/waybar/."
  echo "==> To use it, copy it to your home directory:"
  echo "    cp -r /etc/skel/.config/waybar ~/.config/"
}
